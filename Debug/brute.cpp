#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    std::freopen("input.txt", "r", stdin);
    std::freopen("output_brute.txt", "w", stdout);

    int t = 1;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}
