#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; cin >> n;
    array<int, 4> a;
    for (int &x: a) {
        cin >> x;
    }

    array<string, 4> b;
    for (auto &x: b) {
        cin >> x;
    }

    

    return 0;
}
