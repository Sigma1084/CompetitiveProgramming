#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int a, b, c, d, m;
    cin >> a >> b >> c >> d >> m;

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;

    while (t--) {
        solve();
        cout << '\n';
    }

    return 0;
}
