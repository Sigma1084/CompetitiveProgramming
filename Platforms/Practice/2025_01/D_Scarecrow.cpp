#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int n, k, l;
    cin >> n >> k >> l;

    vector<int> a(n);
    for (auto &x: a) {
        cin >> x;
    }

    
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t = 1;
    std::cin >> t;

    while (t--) {
        solve();
        std::cout << '\n';
    }

    return 0;
}
