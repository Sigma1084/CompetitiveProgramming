#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    string s, t;
    cin >> s >> t;

    if (s == "AtCoder" and t == "Land") {
        cout << "Yes";
    } else {
        cout << "No";
    }

    return 0;
}
