#include <bits/stdc++.h>
using namespace std;
using ll = int64_t;


/**
 * dp[reversed(10010)] := Answer using cows 0 and 3
 */
int main() {
	ios::sync_with_stdio(false); cin.tie(nullptr);

	#ifndef ONLINE_JUDGE
		freopen("guard.in", "r", stdin);
		freopen("guard.out", "w", stdout);
	#endif

	int n, H; cin >> n >> H;

	// [h, w, s]
	vector<array<int, 3>> cows;
	for (auto &[h, w, s]: cows) cin >> h >> w >> s;


	
	return 0;
}
