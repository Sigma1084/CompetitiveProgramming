#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
  double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
    /**
         * @brief
         * - After combining, we will have m + n elements to deal with
         * - If we find the k-th smallest number in the combined array, 
         *   we can find the median for both even and odd cases
         * - The explanation of the k-th smallest is explained in the function
     */
  }
    
  int kthSmallest(const vector<int> &a, const vector<int> &b) {
    /**
         * @brief 
         * - Calculates the k-th smallest element in the combination of the 2 arrays
         * - 
         * 
         * 
         * @return The k-th smallest in the combination of the 2 arrays
     */
  }
};